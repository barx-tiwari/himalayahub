export const programmingNotes = [
  {
    id: 'javascript',
    title: 'JavaScript',
    category: 'programming',
    summary: 'The language of the web: variables, functions, objects and async code.',
    tags: ['js', 'web', 'es6', 'async'],
    explanation:
      'JavaScript is a dynamically typed, interpreted (JIT-compiled) language that runs in every web browser and, through Node.js, on servers. It supports imperative, object-oriented and functional styles.\n\nJavaScript is single-threaded but non-blocking: long tasks such as network requests are handled asynchronously through the event loop, promises and async/await.',
    diagram: {
      type: 'code',
      title: 'Core syntax',
      content: 'const user = { name: "Sita", marks: [78, 91, 85] };\nconst avg = (arr) => arr.reduce((a, b) => a + b, 0) / arr.length;\nconsole.log(`${user.name}: ${avg(user.marks).toFixed(1)}`);\n\nasync function load() {\n  const res = await fetch("/api/notes");\n  return res.json();\n}',
    },
    keyPoints: [
      'let and const are block-scoped; avoid var.',
      'Primitive types: string, number, bigint, boolean, undefined, null, symbol.',
      '=== compares without type conversion; prefer it over ==.',
      'Functions are first-class: they can be stored, passed and returned.',
      'Closures let inner functions remember variables from their outer scope.',
      'Array methods: map, filter, reduce, find, some, every.',
    ],
    examples: ['[1, 2, 3].map((n) => n * 2) returns [2, 4, 6].'],
    examQuestions: ['What is a closure? Give an example.', 'Explain the event loop.', 'Differentiate let, const and var.', 'What is the difference between == and ===?'],
    revision: ['Dynamic, single-threaded, event loop.', 'Use const/let and ===.', 'Promises and async/await for async work.'],
  },
  {
    id: 'react',
    title: 'React',
    category: 'programming',
    summary: 'A library for building UIs from components, props and state.',
    tags: ['javascript', 'frontend', 'hooks', 'jsx'],
    explanation:
      'React builds user interfaces out of components: functions that take props and return JSX describing what should appear. When state changes, React re-renders the component and efficiently updates only what changed in the DOM.\n\nHooks such as useState and useEffect let function components hold state and run side effects.',
    diagram: {
      type: 'code',
      title: 'A stateful component',
      content: 'import { useState } from "react";\n\nexport default function Counter() {\n  const [count, setCount] = useState(0);\n  return (\n    <button onClick={() => setCount(count + 1)}>\n      Clicked {count} times\n    </button>\n  );\n}',
    },
    keyPoints: [
      'Props flow down from parent to child; they are read-only.',
      'State is private to a component and triggers re-renders when updated.',
      'useEffect runs side effects (fetching, subscriptions) after render.',
      'Lists need a stable key prop.',
      'Lift state up to share it between siblings; use Context for app-wide values.',
    ],
    examples: ['This HimalayaHub app is built with React, React Router and Context.'],
    examQuestions: ['Differentiate props and state.', 'What is the virtual DOM?', 'Explain useState and useEffect with examples.'],
    revision: ['Components + props + state.', 'Hooks start with use.', 'Keys for lists.'],
  },
  {
    id: 'python',
    title: 'Python',
    category: 'programming',
    summary: 'Readable, versatile language for scripting, data and AI.',
    tags: ['scripting', 'data science', 'beginner'],
    explanation:
      'Python is a high-level, interpreted, dynamically typed language known for clean syntax that uses indentation instead of braces. It has a huge standard library and ecosystem (NumPy, pandas, Django, Flask, TensorFlow).\n\nIt is popular for beginners, automation, web back-ends, data analysis and machine learning.',
    diagram: {
      type: 'code',
      title: 'Lists, dictionaries and functions',
      content: 'marks = {"Ram": 72, "Sita": 88, "Hari": 65}\n\ndef grade(score):\n    return "A" if score >= 80 else "B" if score >= 60 else "C"\n\nfor name, score in marks.items():\n    print(name, grade(score))\n\nsquares = [n * n for n in range(1, 6)]  # list comprehension',
    },
    keyPoints: [
      'Built-in types: int, float, str, bool, list, tuple, set, dict.',
      'Lists are mutable; tuples are immutable.',
      'Indentation defines blocks.',
      'Supports OOP with classes, inheritance and dunder methods like __init__.',
      'Exceptions are handled with try / except / finally.',
    ],
    examples: ['Swap two variables: a, b = b, a'],
    examQuestions: ['Differentiate list and tuple.', 'What is a list comprehension?', 'Explain exception handling in Python.'],
    revision: ['Indentation matters.', 'list mutable, tuple not.', 'dict = key/value.'],
  },
  {
    id: 'java',
    title: 'Java',
    category: 'programming',
    summary: 'Object-oriented, platform-independent language running on the JVM.',
    tags: ['oop', 'jvm', 'classes'],
    explanation:
      'Java source code compiles to bytecode, which runs on the Java Virtual Machine. This makes programs "write once, run anywhere". Java is strongly typed and class-based, and is widely used for Android, enterprise back-ends and large systems.',
    diagram: {
      type: 'code',
      title: 'Class and inheritance',
      content: 'class Animal {\n  String name;\n  Animal(String name) { this.name = name; }\n  void speak() { System.out.println(name + " makes a sound"); }\n}\n\nclass Dog extends Animal {\n  Dog(String name) { super(name); }\n  @Override void speak() { System.out.println(name + " barks"); }\n}',
    },
    keyPoints: [
      'Four OOP pillars: encapsulation, inheritance, polymorphism, abstraction.',
      'JDK (develop) ⊃ JRE (run) ⊃ JVM (execute bytecode).',
      'Garbage collection frees unused memory automatically.',
      'Interfaces define contracts; a class can implement many interfaces.',
      'Method overloading = compile-time polymorphism; overriding = runtime.',
    ],
    examQuestions: ['Explain JDK, JRE and JVM.', 'Differentiate overloading and overriding.', 'Why is Java platform-independent?'],
    revision: ['Bytecode on JVM.', 'OOP pillars.', 'GC manages memory.'],
  },
  {
    id: 'c-language',
    title: 'C Programming',
    category: 'programming',
    summary: 'Procedural, close-to-hardware language with pointers and manual memory.',
    tags: ['pointers', 'procedural', 'systems'],
    explanation:
      'C is a compiled, procedural language created by Dennis Ritchie at Bell Labs in 1972. It gives direct access to memory through pointers, which makes it fast and suitable for operating systems, embedded devices and compilers.',
    diagram: {
      type: 'code',
      title: 'Pointers and functions',
      content: '#include <stdio.h>\n\nvoid swap(int *a, int *b) {\n  int t = *a; *a = *b; *b = t;\n}\n\nint main(void) {\n  int x = 5, y = 9;\n  swap(&x, &y);\n  printf("%d %d\\n", x, y); /* 9 5 */\n  return 0;\n}',
    },
    keyPoints: [
      'A pointer stores a memory address; & gives an address, * dereferences it.',
      'Arrays decay to pointers when passed to functions.',
      'Dynamic memory: malloc, calloc, realloc, free.',
      'Strings are char arrays ending with \\0.',
      'Structures group related variables; unions share memory.',
    ],
    examQuestions: ['What is a pointer? Explain call by value vs call by reference.', 'Differentiate structure and union.', 'Explain dynamic memory allocation.'],
    revision: ['Pointers = addresses.', 'free what you malloc.', 'Strings end with \\0.'],
  },
  {
    id: 'cpp',
    title: 'C++',
    category: 'programming',
    summary: 'C with classes, templates and the Standard Template Library.',
    tags: ['oop', 'stl', 'templates'],
    explanation:
      'C++ extends C with object-oriented features (classes, inheritance, virtual functions), generic programming (templates) and a rich standard library (STL containers, algorithms, iterators). It is used for games, browsers, trading systems and performance-critical software.',
    diagram: {
      type: 'code',
      title: 'STL vector and a class',
      content: '#include <iostream>\n#include <vector>\n#include <algorithm>\n\nint main() {\n  std::vector<int> v = {5, 2, 9, 1};\n  std::sort(v.begin(), v.end());\n  for (int n : v) std::cout << n << " ";\n}',
    },
    keyPoints: [
      'Constructors and destructors manage object lifetime (RAII).',
      'Access specifiers: public, private, protected.',
      'Virtual functions enable runtime polymorphism.',
      'Function and operator overloading.',
      'STL: vector, list, map, set, stack, queue + algorithms.',
    ],
    examQuestions: ['Differentiate C and C++.', 'What are virtual functions?', 'Explain constructors and destructors with an example.'],
    revision: ['C + OOP + templates.', 'RAII.', 'STL containers.'],
  },
  {
    id: 'typescript',
    title: 'TypeScript',
    category: 'programming',
    summary: 'JavaScript with static types that catch mistakes before runtime.',
    tags: ['javascript', 'types', 'frontend'],
    explanation:
      'TypeScript is a superset of JavaScript developed by Microsoft. It adds optional static typing, interfaces and generics, then compiles to plain JavaScript. Editors use the types to offer autocomplete and flag errors as you write.',
    diagram: {
      type: 'code',
      title: 'Types and interfaces',
      content: 'interface Student {\n  id: number;\n  name: string;\n  email?: string; // optional\n}\n\nfunction greet(s: Student): string {\n  return `Hello, ${s.name}`;\n}\n\nconst first = <T,>(items: T[]): T | undefined => items[0];',
    },
    keyPoints: [
      'Types are removed at compile time; no runtime cost.',
      'Union types (string | number) and literal types.',
      'Interfaces and type aliases describe object shapes.',
      'Generics write reusable, type-safe functions.',
      'strict mode catches null/undefined mistakes.',
    ],
    examQuestions: ['Why use TypeScript over JavaScript?', 'Differentiate interface and type alias.', 'What are generics?'],
    revision: ['Superset of JS.', 'Compiles to JS.', 'Types catch bugs early.'],
  },
];
